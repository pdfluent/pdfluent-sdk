{
  "name": "@pdfluent/example-strict-ts",
  "private": true,
  "version": "0.0.1",
  "description": "Strict TypeScript example for @pdfluent/node — tsc --strict --noEmit must pass.",
  "scripts": {
    "typecheck": "tsc --strict --noEmit"
  },
  "_comment": "During beta development @pdfluent/node resolves via the tsconfig.json 'paths' entry pointing at ../../../crates/pdf-node/index.d.ts. Switch to the published pin once on npm: \"@pdfluent/node\": \"1.0.0-beta.8\".",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
